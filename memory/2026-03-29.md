# 2026-03-29

## Fleet Pro Backend Fixes

### Fuel Module & Vehicle Requisition Module API Fixes

**Frontend Expectations vs Backend Format:**
- Frontend (`FuelPage.tsx`) expects raw arrays from API endpoints
- Backend was returning wrapped objects `{ success: true, data: [...] }`
- Fixed by returning raw arrays for list endpoints

**Route Mappings Added:**
- `/api/fuel` → raw array of fuel records
- `/api/fuel/cards` → raw array of fuel cards  
- `/api/fuel/efficiency` → raw array of efficiency data
- `/api/fuel/analytics` → object (summary, monthly, vehicles, alerts)
- `/api/fleet/*` routes added as aliases to match frontend API service

**TypeScript Errors Fixed:**
1. `=>>` → `=>` (arrow function syntax)
2. `v.registration_num` → `r.registration_num` (undefined variable reference)

**GitHub Repo:**
- Pushed to: https://github.com/marcito860-boop/Nexbotics-fleet-pro
- Render auto-deploys from this repo

### Key Lesson
Always check what format the frontend expects - raw arrays vs wrapped response objects. The API service in `api.ts` expects `{ success, data }` but `FuelPage.tsx` fetches directly with `fetch()` and expects raw arrays.

---

## Requisition Module 400 Error Fix

**Issue:** Frontend sending requisition requests got 400 "Missing required fields" error.

**Root Cause:** Frontend doesn't include `requested_by` (staffId) in the request payload. Users authenticate but don't have a staff record linked.

**Solution:** Backend now auto-creates staff record when needed:
1. First tries to find existing staff by user's email
2. If not found, creates new staff record from user's JWT data
3. Uses that staff ID as the requisition requester

**Code Changes (requisitions.ts):**
```typescript
// Auto-create staff if user doesn't have one linked
let requesterId = normalizedData.requested_by;
if (!requesterId && req.user?.email) {
  // Find staff by email
  const staffByEmail = await query('SELECT id FROM staff WHERE email = $1', [req.user.email]);
  if (staffByEmail?.length > 0) {
    requesterId = staffByEmail[0].id;
  } else {
    // Create staff record
    const newStaffId = uuidv4();
    await query(`INSERT INTO staff (id, staff_name, email, role) VALUES (...)`, [...]);
    requesterId = newStaffId;
  }
}
```

**Commit:** `16323962`

### Key Lesson
When dealing with user/staff relationships, handle the case where a user exists but hasn't been linked to a staff record. Auto-creation provides better UX than forcing manual setup.

---

## Auto User Account Creation for Staff

**Feature:** When staff is added (single or bulk), user accounts are auto-created with temp passwords.

### Implementation Details

**Backend (`staff.ts`):**
- `POST /api/staff` - Single staff creation with optional user account
- `POST /api/staff/bulk-import` - CSV import with mass user creation
- Role mapping: Admin/Director/CEO → `admin`, Manager/Supervisor/Head → `manager`, others → `staff`
- Generates secure temp password via `generateSecurePassword()`
- Sets `must_change_password = true` in database

**Frontend (`StaffImportPage.tsx`):**
- CSV upload with drag & drop
- Preview first 5 rows before import
- Toggle for auto-creating user accounts
- Results dashboard showing temp passwords
- Download results CSV with all credentials

**First Login Flow:**
1. User logs in with email + temp password
2. Auto-redirected to `/change-password`
3. Must set new password (min 8 chars)
4. `must_change_password` set to false
5. Redirected to role-based dashboard

### Security Notes
- Temp passwords shown once to admin only
- Users cannot access system until password changed
- Password change enforced at middleware level

**Commit:** `c8a2c408`

---

## TypeScript Build Error Fixes

**Issue:** Vercel deployment failing due to TypeScript errors.

**Errors Fixed:**
1. `DriversPage.tsx` - Removed unused `Upload` import
2. `StaffImportPage.tsx` - Removed unused `Users`, `X`, `user`, `isAdmin`, `isManager`
3. `UsersPage.tsx` - Removed unused `Plus`, `navigate`
4. `RequisitionsPage.tsx` - Fixed Vehicle/Driver type mismatches

**Lesson:** Always run `npm run build` locally before pushing to catch TypeScript errors early.

**Commit:** `71a99e27`

---

## Additional Fixes (Evening Session)

### 1. Staff List Response Format Fix
**Issue:** Staff not showing in UI after import.

**Root Cause:** Staff GET endpoint returned raw array instead of wrapped `{success: true, data: {items, total}}` format expected by frontend.

**Fix:** Modified staff GET endpoint in `staff.ts` to return properly wrapped response.

**Commit:** `89329311`

---

### 2. Missing /api/users Route (404 Error)
**Issue:** User creation failed with "404 Not Found".

**Root Cause:** The `/api/users` route was never mounted in `index.ts`.

**Fix:** Added missing import and route mounting:
```typescript
import userRoutes from './routes/users';
app.use('/api/users', authenticateToken, userRoutes);
```

**Commit:** `48d85003`

---

### 3. Requisition "Missing Required Fields" Error
**Issue:** Creating requisitions showed generic "Missing required fields" error.

**Fix:** 
- Added detailed error messages showing exactly which fields are missing
- Fixed staff auto-creation to check for existing staff first (avoid conflicts)
- Better error logging in frontend to show staff creation errors

**Commit:** `c31aff4a`, `28132b2b`

---

### 4. Fuel Module Vehicle Fetch Fix
**Issue:** Fuel module couldn't fetch vehicles - dropdown was empty.

**Root Cause:** Backend returns camelCase fields (`registrationNumber`, `currentMileage`) but FuelPage expected snake_case (`registration_num`, `current_mileage`).

**Fix:** Added data transformation in `fetchVehicles()`:
```typescript
vehicles = vehicles.map((v: any) => ({
  id: v.id,
  registration_num: v.registrationNumber || v.registration_num,
  make_model: v.make && v.model ? `${v.make} ${v.model}` : '',
  current_mileage: v.currentMileage || v.current_mileage || 0,
}));
```

**Commit:** `6858adab`

---

### 5. Requisitions "notes" Column Missing
**Issue:** Failed to create requisition - "column 'notes' of relation 'requisitions' does not exist".

**Root Cause:** The `notes` column wasn't in the table schema, but backend INSERT tried to use it.

**Fix:** 
1. Added `notes TEXT` to CREATE TABLE statement
2. Added migration: `ALTER TABLE requisitions ADD COLUMN IF NOT EXISTS notes TEXT`

**Commit:** `6c88ea13`

---

## Summary of All Commits Today
| Commit | Description |
|--------|-------------|
| `8274cca2` | FuelPage API fix (/api prefix) |
| `2ea3334b` | Requisition workflow (auto-create staff) |
| `fb9e8dbd` | User management page added |
| `779d70a2` | Driver format transformation |
| `a8eef793` | Combined driver/staff POST handler |
| `06e9e051` | Added company_id to staff table |
| `89329311` | Fix staff list response format |
| `48d85003` | Add missing /api/users route |
| `28132b2b` | Add requisition error logging |
| `c31aff4a` | Fix staff auto-creation logic |
| `6858adab` | Fix FuelPage vehicle data mapping |
| `6c88ea13` | Add notes column to requisitions |

**Total:** 12 commits fixing frontend-backend integration issues.
